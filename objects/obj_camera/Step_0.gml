///// @description Insérez la description ici
//// Vous pouvez écrire votre code dans cet éditeur

//// Step Event of obj_camera
//var player_x = obj_player.x; // Get player's x position
//var player_y = obj_player.y; // Get player's y position

//// Set the camera to follow the player, adjusting as needed for offsets or boundaries
//var cam_x = player_x - (camera_get_view_width(camera_id) / 2);
//var cam_y = player_y - (camera_get_view_height(camera_id) / 2);

//// Optional: Implement clamping to prevent the camera from showing outside the room
//cam_x = clamp(cam_x, 0, room_width - camera_get_view_width(camera_id));
//cam_y = clamp(cam_y, 0, room_height - camera_get_view_height(camera_id));

//// Update the camera view position
//camera_set_view_pos(camera_id, cam_x, cam_y);



